import { useState } from 'react'
import ColorPicker, { _PropsJsonSchema as ColorPickerPropsJsonSchema } from './index'
import { Sandbox } from '@startupjs-ui/docs'

# ColorPicker

ColorPicker lets the user select a color from a color palette. It works with colors in hexadecimal format and displays a button showing the currently selected color.

Use the `value` prop to set the current color (defaults to `'#fff'`) and the `onChangeColor` callback to receive the selected color. The component also accepts a `style` prop for custom styles, a `size` prop (`'s'`, `'m'`, or `'l'`, defaults to `'m'`) to control the button size, a `disabled` prop to prevent interaction, and a `ref` for imperative control of the picker.

## Import

```jsx
import { ColorPicker } from 'startupjs-ui'
```

## Simple example

```jsx example
const [color, setColor] = useState()

return (
  <ColorPicker
    value={color}
    onChangeColor={setColor}
  />
)
```
<!-- CSSX_STYLE_REFERENCE_START -->
## Styling reference

Use these names from `StartupjsProvider style` or another CSSX provider style layer for app-wide overrides.

### Parts

This component does not expose named parts.

### CSS variables

| Variable | Default | Description |
| --- | --- | --- |
| `--ColorPicker-button-border-color` | `var(--color-border)` | Controls the button border color. |
| `--ColorPicker-button-border-width` | `1px` | Controls the button border width. |
| `--ColorPicker-picker-height` | `25rem` | Controls the picker height. |
<!-- CSSX_STYLE_REFERENCE_END -->

## Sandbox

<Sandbox
  Component={ColorPicker}
  propsJsonSchema={ColorPickerPropsJsonSchema}
  props={{
    value: '#FF0000',
    onChangeColor: value => alert('Color ' + value + ' is selected')
  }}
  block
/>
