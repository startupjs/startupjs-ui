import Link, { _PropsJsonSchema as LinkPropsJsonSchema } from './index'
import Br from '@startupjs-ui/br'
import Button from '@startupjs-ui/button'
import Div from '@startupjs-ui/div'
import Rating from '@startupjs-ui/rating'
import { Sandbox } from '@startupjs-ui/docs'

# Link

Link provides navigation within the app and to external URLs. Use the `to` prop (or its alias `href`) to specify the destination path or URL.

The component also accepts a `style` prop for custom styles, a `theme` prop for theme-based styling, `push` to use router push instead of navigate, and `replace` to replace the current history entry instead of adding a new one.

```jsx
import { Link } from 'startupjs-ui'
```

## Simple example

```jsx example
return (
  <Link to='/docs/Card'>
    Card
  </Link>
)
```

## Different display

The `display` prop has two options: `'inline'` and `'block'`. If not specified, it is auto-detected based on the type of `children` -- string children render as `'inline'`, and everything else renders as `'block'`. The two display modes have different visual appearances.

```jsx example
return (
  <Div>
    <Link to='/docs/Card'>
      Card
    </Link>
    <Br />
    <Link to='/docs/Rating'>
      <Rating value={4.5} readonly />
    </Link>
  </Div>
)
```

## Colors

To change the link color, pass a CSSX color token to the `color` prop. This only applies when `display` is `'inline'`.

```jsx example
return (
  <Link
    to='/docs/Card'
    color='primary'
  >
    Card
  </Link>
)
```

## Bold link

To make the link text bold, pass the `bold` prop.

```jsx example
return (
  <Link to='/docs/Card' bold>
    Card
  </Link>
)
```

## Italic link

To make the link text italic, pass the `italic` prop.

```jsx example
return (
  <Link to='/docs/Card' italic>
    Card
  </Link>
)
```

## Hover style

You can change the hover style like in the [Div component](/docs/Div) by passing `variant='highlight'`. This only applies when `display` is `'block'`.

```jsx example
const style = {
  height: 80,
  backgroundColor: 'white',
  alignItems: 'center',
  justifyContent: 'center'
}
return (
  <Link
    style={style}
    to='/docs/Rating'
    variant='highlight'
  >
    <Rating value={4.5} readonly />
  </Link>
)
```

## Button as link

To use a button as a link, wrap a Button component inside Link. The link will automatically pass its props through to the button.

```jsx example
return (
  <Link
    to='/docs/Card'
  >
    <Button>Card</Button>
  </Link>
)
```

## Actions

By passing an `onPress` handler, you can run custom logic before the navigation occurs.

```jsx example
function onPress (event) {
  console.log('Go to alert docs')
}
return (
  <Link
    color='primary'
    to='/docs/Alert'
    onPress={onPress}
  >
    Alert docs
  </Link>
)
```

## Styling reference

Use these names from `StartupjsProvider style` or another CSSX provider style layer for app-wide overrides.
For the full theming model, see the [Styling and theming](/docs/Styling) guide.

### Parts

| Part | Description |
| --- | --- |
| `root` | Root element. |

### CSS variables

| Variable | Default | Description |
| --- | --- | --- |
| `--Link-color` | `var(--color-primary)` | Controls the color. |
| `--Link-primary-color` | `var(--color-primary)` | Controls the primary color. |
| `--Link-decoration-color` | `var(--Link-color)` | Controls the decoration color. |
| `--Link-primary-decoration-color` | `var(--Link-primary-color)` | Controls the primary decoration color. |

## Sandbox

<Sandbox
  Component={Link}
  propsJsonSchema={LinkPropsJsonSchema}
  props={{
    to: '#',
    children: 'StartupJS'
  }}
/>
