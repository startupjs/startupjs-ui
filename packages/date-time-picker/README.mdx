import { useState } from 'react'
import { $ } from 'startupjs'
import DateTimePicker, { _PropsJsonSchema as DateTimePickerPropsJsonSchema } from './index'
import { Sandbox } from '@startupjs-ui/docs'

# DateTimePicker

DateTimePicker allows the user to display and select a date and time. It renders a text input that opens a picker when tapped.

Use the `date` prop to set the current value as a timestamp and the `onChangeDate` callback to receive the selected timestamp. The picker supports several modes via the `mode` prop: `'date'`, `'time'`, `'datetime'` (default), or `'countdown'`.

Additional props include `style` for custom root styles, `contentStyle` for the picker content container, `size` (`'s'`, `'m'`, `'l'`, defaults to `'m'`) for the input size, `display` for the native picker display mode (defaults to `'spinner'`), `dateFormat` for custom date formatting (using moment format strings), `locale` for localization (falls back to the device locale), and `timezone` for specifying an IANA timezone (defaults to auto-detected).

You can constrain the selectable range with `minDate` and `maxDate` (both timestamps), disable specific days with `disabledDays`, set a date range highlight with `range`, control the time step with `timeInterval` (defaults to `5` minutes), and force 24-hour format with `is24Hour`. The component also supports `disabled`, `readonly`, `placeholder`, `renderInput` for a custom input renderer, `ref` for imperative access, and `testID`/`calendarTestID` for testing.

```jsx
import { DateTimePicker } from 'startupjs-ui'
```

## Simple example

```jsx example
const [date, setDate] = useState(+new Date())

return (
  <DateTimePicker
    date={date}
    onChangeDate={setDate}
  />
)
```

## Managing visibility

There are three ways to manage the visibility of a DateTimePicker:

1. **Scoped model** -- pass a scoped model to the `$visible` prop, which automatically controls the picker's open/close state.

```jsx example
const $visible = $()
const [date, setDate] = useState()

return (
  <DateTimePicker
    date={date}
    $visible={$visible}
    onChangeDate={setDate}
  />
)
```

2. **Controlled** -- pass the `visible`, `onRequestOpen`, and `onRequestClose` props to manually control when the picker opens and closes.

```jsx example
const $visible = $()
const [date, setDate] = useState()
const visible = $visible.get()

return (
  <DateTimePicker
    date={date}
    visible={visible}
    onChangeDate={setDate}
    onRequestOpen={() => $visible.set(true)}
    onRequestClose={() => $visible.del()}
  />
)
```

3. **Uncontrolled** -- if neither `$visible` nor `visible` is provided, the component manages its own visibility internally.

```jsx example
const [date, setDate] = useState()

return (
  <DateTimePicker
    date={date}
    onChangeDate={setDate}
  />
)
```
<!-- CSSX_STYLE_REFERENCE_START -->
## Styling reference

Use these names from `StartupjsProvider style` or another CSSX provider style layer for app-wide overrides.

### Parts

This component does not expose named parts.

### CSS variables

| Variable | Default | Description |
| --- | --- | --- |
| `--DateTimePicker-content-padding` | `0.5rem` | Controls the content padding. |
| `--DateTimePicker-content-tablet-height` | `21rem` | Controls the content tablet height. |
| `--DateTimePicker-divider-width` | `60%` | Controls the divider width. |
| `--DateTimePicker-divider-height` | `1px` | Controls the divider height. |
| `--DateTimePicker-divider-margin-y` | `0.5rem` | Controls the vertical divider margin. |
| `--DateTimePicker-divider-tablet-height` | `95%` | Controls the divider tablet height. |
| `--DateTimePicker-divider-tablet-width` | `1px` | Controls the divider tablet width. |
| `--DateTimePicker-divider-tablet-margin-x` | `1rem` | Controls the horizontal divider tablet margin. |
| `--DateTimePicker-drawer-padding-top` | `1.5rem` | Controls the drawer padding top. |
| `--DateTimePicker-rn-picker-margin-top` | `1rem` | Controls the React Native picker margin top. |
| `--DateTimePicker-swipe-height` | `1.5rem` | Controls the swipe height. |
| `--DateTimePicker-popover-bg` | `var(--color-popover)` | Controls the popover background. |
| `--DateTimePicker-popover-radius` | `var(--radius-md)` | Controls the popover radius. |
| `--DateTimePicker-popover-shadow` | `var(--shadow-md)` | Controls the popover shadow. |
| `--DateTimePicker-muted-color` | `var(--color-muted-foreground)` | Controls the muted color. |
| `--DateTimePicker-primary-text-color` | `var(--color-primary)` | Controls the primary text color. |
| `--DateTimePicker-active-color` | `var(--color-primary-foreground)` | Controls the active color. |
| `--DateTimePicker-hover-bg` | `color-mix(in srgb, var(--color-primary) 5%, transparent)` | Controls the hover background. |
| `--DateTimePicker-active-bg` | `var(--color-primary)` | Controls the active background. |
| `--DateTimePicker-active-hover-bg` | `color-mix(in srgb, var(--color-primary) 70%, transparent)` | Controls the active hover background. |
| `--DateTimePicker-range-bg` | `color-mix(in srgb, var(--color-primary) 20%, transparent)` | Controls the range background. |
| `--DateTimePicker-caption-font-size` | `var(--text-xs)` | Controls the caption font size. |
| `--DateTimePicker-caption-line-height` | `1rem` | Controls the caption line height. |
| `--DateTimePicker-heading-font-size` | `var(--text-xl)` | Controls the heading font size. |
| `--DateTimePicker-heading-line-height` | `1.75rem` | Controls the heading line height. |
| `--DateTimePicker-day-cell-size` | `2rem` | Controls the day cell size. |
| `--DateTimePicker-day-cell-margin` | `0.25rem` | Controls the day cell margin. |
| `--DateTimePicker-day-cell-radius` | `9999px` | Controls the day cell radius. |
| `--DateTimePicker-header-margin-left` | `0.5rem` | Controls the header margin left. |
| `--DateTimePicker-header-margin-bottom` | `0.25rem` | Controls the header margin bottom. |
| `--DateTimePicker-header-actions-margin-left` | `1rem` | Controls the header actions margin left. |
| `--DateTimePicker-header-button-margin-left` | `0.25rem` | Controls the header button margin left. |
| `--DateTimePicker-years-popover-max-height` | `13.5rem` | Controls the years popover max height. |
| `--DateTimePicker-years-item-height` | `2.25rem` | Controls the years item height. |
| `--DateTimePicker-years-item-padding-x` | `1rem` | Controls the horizontal years item padding. |
| `--DateTimePicker-time-cell-height` | `2.5rem` | Controls the time cell height. |
| `--DateTimePicker-time-cell-width` | `10rem` | Controls the time cell width. |
| `--DateTimePicker-time-cell-width-tablet` | `6rem` | Controls the time cell width tablet. |
<!-- CSSX_STYLE_REFERENCE_END -->

## Sandbox

<Sandbox
  Component={DateTimePicker}
  propsJsonSchema={DateTimePickerPropsJsonSchema}
  props={{
    date: +new Date()
  }}
  block
/>
