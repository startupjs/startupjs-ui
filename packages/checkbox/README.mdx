import { useState } from 'react'
import { faMinus } from '@fortawesome/free-solid-svg-icons/faMinus'
import Checkbox, { _PropsJsonSchema as CheckboxPropsJsonSchema } from './index'
import Div from '@startupjs-ui/div'
import Br from '@startupjs-ui/br'
import { Sandbox } from '@startupjs-ui/docs'

# Checkbox

Checkboxes allow the user to select one or more items from a set. The component supports two visual variants: a standard checkbox and a toggle switch.

Use the `value` prop to control the checked state and the `onChange` prop to handle state changes. The component also accepts `style` for the outer wrapper, `inputStyle` for the input element itself, and `switchCircleStyle` to customize the switch thumb when using the `'switch'` variant.

```jsx
import { Checkbox } from 'startupjs-ui'
```

## Simple example

```jsx example
const [checked, setChecked] = useState(false)

return (
  <Div>
    <Checkbox
      value={checked}
      onChange={setChecked}
    />
    <Br />
    <Checkbox
      variant='switch'
      value={checked}
      onChange={setChecked}
    />
  </Div>
)
```

## Variants

There are two display variants: `'checkbox'` and `'switch'`. The default is `'checkbox'`.

```jsx example
const [checked, setChecked] = useState(false)

return (
  <Checkbox
    variant='switch'
    value={checked}
    onChange={setChecked}
  />
)
```

## Disabled

When the `disabled` prop is set, the checkbox becomes non-interactive and appears dimmed.

```jsx example
return (
  <Checkbox disabled />
)
```

## Readonly

When the `readonly` prop is set, the checkbox displays its current value as a non-interactive text symbol.

```jsx example
return (
  <Checkbox readonly />
)
```

## Checkbox with custom icon

The `icon` prop allows you to replace the default checkmark icon.

```jsx example
const [checked, setChecked] = useState(false)

return (
  <Checkbox
    value={checked}
    icon={faMinus}
    onChange={setChecked}
  />
)
```
<!-- CSSX_STYLE_REFERENCE_START -->
## Styling reference

Use these names from `StartupjsProvider style` or another CSSX provider style layer for app-wide overrides.

### Parts

| Part | Description |
| --- | --- |
| `root` | Root element. |
| `icon` | Primary icon. |
| `input` | Input control. |
| `readonly` | Readonly value. |
| `switchCircle` | Switch thumb. |

### CSS variables

| Variable | Default | Description |
| --- | --- | --- |
| `--Checkbox-readonly-font-size` | `1rem` | Controls the readonly font size. |
| `--Checkbox-readonly-line-height` | `1rem` | Controls the readonly line height. |
| `--Checkbox-readonly-font-weight` | `var(--font-weight-black)` | Controls the readonly font weight. |
| `--Checkbox-size` | `1rem` | Controls the size. |
| `--Checkbox-border-width` | `2px` | Controls the border width. |
| `--Checkbox-border-color` | `var(--color-border)` | Controls the border color. |
| `--Checkbox-radius` | `var(--radius-xs)` | Controls the radius. |
| `--Checkbox-checked-bg` | `var(--color-primary)` | Controls the checked background. |
| `--Checkbox-checked-border-color` | `var(--color-primary)` | Controls the checked border color. |
| `--Checkbox-checked-icon-color` | `var(--color-primary-foreground)` | Controls the checked icon color. |
| `--Checkbox-error-border-color` | `var(--color-destructive)` | Controls the error border color. |
| `--Checkbox-switch-width` | `1.5rem` | Controls the switch width. |
| `--Checkbox-switch-height` | `1rem` | Controls the switch height. |
| `--Checkbox-switch-radius` | `9999px` | Controls the switch radius. |
| `--Checkbox-switch-bg` | `var(--color-muted-foreground)` | Controls the switch background. |
| `--Checkbox-switch-bg-error` | `var(--color-destructive)` | Controls the switch background error. |
| `--Checkbox-switch-bg-checked` | `var(--color-primary)` | Controls the switch background checked. |
| `--Checkbox-switch-circle-size` | `10px` | Controls the switch circle size. |
| `--Checkbox-switch-circle-bg` | `var(--color-background)` | Controls the switch circle background. |
| `--Checkbox-switch-circle-bg-checked` | `var(--color-primary-foreground)` | Controls the switch circle background checked. |
| `--Checkbox-switch-circle-radius` | `9999px` | Controls the switch circle radius. |
| `--Checkbox-switch-circle-shadow` | `var(--shadow-xs)` | Controls the switch circle shadow. |
<!-- CSSX_STYLE_REFERENCE_END -->

## Sandbox

<Sandbox
  Component={Checkbox}
  propsJsonSchema={CheckboxPropsJsonSchema}
  extraParams={{
    icon: {
      showIconSelect: true
    }
  }}
  props={{
    onChange: value => alert('New value is ' + value)
  }}
/>
