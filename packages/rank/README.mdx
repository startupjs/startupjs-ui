import { useState } from 'react'
import Rank, { _PropsJsonSchema as RankPropsJsonSchema } from './index'
import { Sandbox } from '@startupjs-ui/docs'

# Rank

Rank is an input component that lets users arrange a list of options in a specific order. Items can be reordered by dragging and dropping or by selecting a position from a numbered dropdown.

The `options` prop defines the available items (as strings, numbers, or `{ value, label }` objects), while `value` holds the current ordering. Use `onChange` to receive the updated order whenever the user rearranges items. You can also apply custom `style` to the root element.

```jsx
import { Rank } from 'startupjs-ui'
```

## Simple example

Pass an array of options and track their current order with `value` and `onChange`.

```jsx example
const options = ['option1', 'option2', 'option3', 'option4', 'option5']
const [value, setValue] = useState(options)

return (
  <Rank
    value={value}
    options={options}
    onChange={setValue}
  />
)
```

## Disabled

Set the `disabled` prop to prevent users from reordering items. The drag handles and position dropdowns become inactive, and the background is dimmed to indicate the disabled state.

```jsx example
const options = ['option1', 'option2', 'option3', 'option4', 'option5']
const [value, setValue] = useState(options)

return (
  <Rank
    disabled
    value={value}
    options={options}
    onChange={setValue}
  />
)
```

## Readonly

Set the `readonly` prop to display the ranked list as a simple numbered list without any interactive controls. This is useful for showing the final ranking result.

```jsx example
const options = ['option1', 'option2', 'option3', 'option4', 'option5']
const [value, setValue] = useState(options)

return (
  <Rank
    readonly
    value={value}
    options={options}
    onChange={setValue}
  />
)
```

## Styling reference

Use these names from `StartupjsProvider style` or another CSSX provider style layer for app-wide overrides.
For the full theming model, see the [Styling and theming](/docs/Styling) guide.

### Parts

This component does not expose named parts.

### CSS variables

| Variable | Default | Description |
| --- | --- | --- |
| `--Rank-item-padding` | `0.5rem` | Controls the item padding. |
| `--Rank-item-gap` | `0.5rem` | Controls the item gap. |
| `--Rank-item-radius` | `var(--radius-md)` | Controls the item radius. |
| `--Rank-item-border-width` | `1px` | Controls the item border width. |
| `--Rank-item-bg` | `var(--color-background)` | Controls the item background. |
| `--Rank-item-bg-disabled` | `var(--color-muted)` | Controls the item background disabled. |
| `--Rank-item-border-color` | `var(--color-border)` | Controls the item border color. |
| `--Rank-icon-disabled-color` | `var(--color-muted-foreground)` | Controls the icon disabled color. |
| `--Rank-select-width` | `12rem` | Controls the position selector width. |
| `--Rank-span-offset` | `1.25rem` | Controls the span offset. |
| `--Rank-right-offset` | `2rem` | Controls the right offset. |
| `--Rank-readonly-index-width` | `1.25rem` | Controls the readonly index width. |
| `--Rank-hint-font-size` | `var(--text-xs)` | Controls the hint font size. |
| `--Rank-hint-line-height` | `1rem` | Controls the hint line height. |

## Sandbox

<Sandbox
  Component={Rank}
  propsJsonSchema={RankPropsJsonSchema}
  props={{
    options: ['option1', 'option2', 'option3', 'option4', 'option5'],
    onChange: value => alert('New order: ' + JSON.stringify(value))
  }}
  block
/>
