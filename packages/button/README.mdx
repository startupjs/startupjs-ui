import { useState } from 'react'
import { pug } from 'startupjs'
import Button, { _PropsJsonSchema as ButtonPropsJsonSchema } from './index'
import Div from '@startupjs-ui/div'
import Br from '@startupjs-ui/br'
import { Sandbox } from '@startupjs-ui/docs'
import { faHouseUser } from '@fortawesome/free-solid-svg-icons/faHouseUser'
import { faCheck } from '@fortawesome/free-solid-svg-icons/faCheck'
import { faCoffee } from '@fortawesome/free-solid-svg-icons/faCoffee'

# Button

Buttons let users take actions or navigate to other pages. They support multiple visual styles to suit different needs and are ideal for drawing attention to the next step in a workflow.

```jsx
import { Button } from 'startupjs-ui'
```

## Simple example

```jsx example
const [counter, setCounter] = useState(0)

return (
  <Button onPress={() => setCounter(counter + 1)}>
    Clicked {counter} times
  </Button>
)
```

## Async button

When the `onPress` handler returns a Promise, the button automatically shows a loading indicator until the Promise resolves. The button is disabled while loading to prevent duplicate submissions.

```jsx example
async function onPress() {
  await new Promise((resolve, reject) => {
    setTimeout(() => {
      resolve()
    }, 5000)
  })
}

return (
  <Button onPress={onPress}>
    Async button
  </Button>
)
```

## Variants

The `variant` prop controls the button's visual style. It affects the background, border, and text/icon colors. The default value is `outlined`.

- **outlined** -- a bordered button with no background fill (default)
- **flat** -- a solid, filled button
- **text** -- a borderless button with no background

```jsx example
return pug`
  Div
    Button(
      onPress=() => console.log('pressed')
      icon=faCoffee
    ) Outlined
    Br
    Button(
      onPress=() => console.log('pressed')
      variant='flat'
      icon=faCoffee
    ) Flat
    Br
    Button(
      onPress=() => console.log('pressed')
      variant='text'
      icon=faCoffee
    ) Text
`
```

## Colors

The default color is `secondary`. You can change it by passing any CSSX color token, such as `primary`, `success`, `warning`, or `error`, to the `color` prop. Tokens resolve through variables like `--color-primary`.

```jsx example
return (
  <Div>
    <Button onPress={() => console.log('pressed')} variant='flat'>
      Default dark
    </Button>
    <Br />
    <Button onPress={() => console.log('pressed')} color='primary' variant='flat'>
      Primary
    </Button>
    <Br />
    <Button onPress={() => console.log('pressed')} color='success' variant='flat'>
      Success
    </Button>
    <Br />
    <Button onPress={() => console.log('pressed')} color='error' variant='flat'>
      Error
    </Button>
  </Div>
)
```

## Sizes

The `size` prop controls the overall button dimensions, including the icon and label text. The default value is `m`. Available sizes are `xs`, `s`, `m`, `l`, `xl`, and `xxl`. See all available values in the Sandbox section at the bottom of the page.

```jsx example
return (
  <Div>
    <Button variant='flat' size='s' icon={faCheck}>
      Small
    </Button>
    <Br />
    <Button variant='flat' icon={faCheck}>
      Default
    </Button>
    <Br />
    <Button variant='flat' size='l' icon={faCheck}>
      Large
    </Button>
  </Div>
)
```

## Shape

The `shape` prop controls the border radius of the button. The default value is `rounded`.

- **rounded** -- standard rounded corners (default)
- **squared** -- sharp, square corners
- **circle** -- fully circular (best for icon-only buttons)

## Icons

You can add an icon using the `icon` prop. To change the icon's position relative to the label, use the `iconPosition` prop (`left` by default).

```jsx example
return (
  <Div>
    <Button icon={faCoffee} iconStyle={{ color: '#16a34a' }}>
      Icon on the left (default) with the changed color
    </Button>
    <Br />
    <Button icon={faCoffee} iconPosition='right'>
      Icon on the right
    </Button>
  </Div>
)
```

## Disabled state

Set the `disabled` prop to `true` to prevent user interaction. A disabled button ignores all press events and appears visually muted.

## Custom styles

You can fine-tune the button's appearance using style props:

- **style** -- custom styles for the root container
- **textStyle** -- custom styles for the label text
- **iconStyle** -- custom styles for the icon
- **hoverStyle** -- custom styles applied on hover
- **activeStyle** -- custom styles applied when pressed


## Call to Action

Try to keep the variety of button combinations you use in your app to a minimum.

Use a regular Button without any extra options by default. Whenever you want to emphasize a particular call to action, use a primary flat button.

```jsx example
<Button color='primary' variant='flat'>
  Destroy the Death Star!
</Button>
```

## Button order

When placing several buttons on one line, follow the ordering conventions that users expect from common operating systems: put `Cancel` first, then `OK`.

Avoid placing multiple call-to-action buttons in the same component, and try to limit the number of them visible at once on a single page.

It is a much better UX practice to use regular buttons by default and decide which one to make a call to action later.

```jsx example
<Div row align='right'>
  <Button>
    Cancel
  </Button>
  <Button pushed color='primary' variant='flat'>
    OK
  </Button>
</Div>
```

## Styling reference

Use these names from `StartupjsProvider style` or another CSSX provider style layer for app-wide overrides.
For the full theming model, see the [Styling and theming](/docs/Styling) guide.

### Parts

| Part | Description |
| --- | --- |
| `root` | Root element. |
| `icon` | Primary icon. |
| `loader` | Loading indicator. |
| `text` | Main text label. |

### CSS variables

| Variable | Default | Description |
| --- | --- | --- |
| `--Button-height-xs` | `1rem` | Controls the xs height. |
| `--Button-height-s` | `1.5rem` | Controls the small height. |
| `--Button-height-m` | `2rem` | Controls the medium height. |
| `--Button-height-l` | `2.5rem` | Controls the large height. |
| `--Button-height-xl` | `3rem` | Controls the xl height. |
| `--Button-height-xxl` | `3.5rem` | Controls the xxl height. |
| `--Button-font-size-xs` | `var(--text-xs)` | Controls the xs font size. |
| `--Button-font-size-s` | `var(--text-xs)` | Controls the small font size. |
| `--Button-font-size-m` | `var(--text-sm)` | Controls the medium font size. |
| `--Button-font-size-l` | `var(--text-base)` | Controls the large font size. |
| `--Button-font-size-xl` | `var(--text-xl)` | Controls the xl font size. |
| `--Button-font-size-xxl` | `var(--text-2xl)` | Controls the xxl font size. |
| `--Button-line-height-xs` | `0.875rem` | Controls the xs line height. |
| `--Button-line-height-s` | `1rem` | Controls the small line height. |
| `--Button-line-height-m` | `1.25rem` | Controls the medium line height. |
| `--Button-line-height-l` | `1.5rem` | Controls the large line height. |
| `--Button-line-height-xl` | `1.75rem` | Controls the xl line height. |
| `--Button-line-height-xxl` | `2rem` | Controls the xxl line height. |
| `--Button-font-weight` | `var(--font-weight-medium)` | Controls the font weight. |
| `--Button-outlined-border-width` | `1px` | Controls the outlined border width. |
| `--Button-outlined-border-color` | `transparent` | Controls the outlined border color. |
| `--Button-web-native-button` | `1` | Controls the web native button. |
| `--Button-disabled-opacity` | `0.25` | Controls the disabled opacity. |
| `--Button-icon-margin-xs` | `calc(var(--spacing) * 1)` | Controls the xs icon margin. |
| `--Button-icon-margin-s` | `calc(var(--spacing) * 1)` | Controls the small icon margin. |
| `--Button-icon-margin-m` | `calc(var(--spacing) * 2)` | Controls the medium icon margin. |
| `--Button-icon-margin-l` | `calc(var(--spacing) * 2)` | Controls the large icon margin. |
| `--Button-icon-margin-xl` | `calc(var(--spacing) * 3)` | Controls the xl icon margin. |
| `--Button-icon-margin-xxl` | `calc(var(--spacing) * 3)` | Controls the xxl icon margin. |

## Sandbox

<Sandbox
  Component={Button}
  propsJsonSchema={ButtonPropsJsonSchema}
  extraParams={{
    icon: {
      showIconSelect: true
    }
  }}
  props={{
    children: 'Press me',
    onPress: () => alert('"onPress" event on "Button" component'),
    onLongPress: () => alert('"onLongPress" event on "Button" component')
  }}
/>
