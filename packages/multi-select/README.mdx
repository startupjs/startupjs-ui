import { useState } from 'react'
import { Sandbox } from '@startupjs-ui/docs'
import Br from '@startupjs-ui/br'
import Div from '@startupjs-ui/div'
import Span from '@startupjs-ui/span'
import MultiSelect, { _PropsJsonSchema as MultiSelectPropsJsonSchema } from './index'

# MultiSelect

MultiSelect lets users pick multiple options from a dropdown list. Selected options are displayed as tags in the input area.

```jsx
import { MultiSelect } from 'startupjs-ui'
```

## Initialization

Before using MultiSelect, you need to configure [Portal](/docs/components/Portal).

## Simple example

```jsx example
const OPTIONS = [
  { label: 'New York', value: 'ny' },
  { label: 'Los Angeles', value: 'la' },
  { label: 'Tokyo', value: 'tk' },
]

const [cities, setCities] = useState([])

return (
  <MultiSelect
    value={cities}
    onChange={setCities}
    options={OPTIONS}
  />
)
```

## Options

You can pass an array of `{ label, value }` objects or an array of primitives to the `options` prop.

```jsx example
const OBJECT_OPTIONS_EXAMPLE = [
  { label: 'New York', value: 'ny' },
  { label: 'Los Angeles', value: 'la' },
]
const PRIMITIVIES_OPTIONS_EXAMPLE = [
  'New York',
  'Los Angeles',
]

const [cities, setCities] = useState([])
const [citiesArray, setCitiesArray] = useState([])

return (
  <>
    <MultiSelect
      value={cities}
      onChange={setCities}
      options={OBJECT_OPTIONS_EXAMPLE}
    />
    <Br />
    <MultiSelect
      value={citiesArray}
      onChange={setCitiesArray}
      options={PRIMITIVIES_OPTIONS_EXAMPLE}
    />
  </>
)
```

## Disabled

Set `disabled` to `true` to prevent all user interaction with the component.

```jsx example
const OPTIONS = [
  { label: 'New York', value: 'ny' },
  { label: 'Los Angeles', value: 'la' },
  { label: 'Tokyo', value: 'tk' },
]
const [cities, setCities] = useState(['ny'])

return (
  <MultiSelect
    disabled
    value={cities}
    onChange={setCities}
    options={OPTIONS}
  />
)
```

## Readonly

Set `readonly` to `true` to display selected values as plain text without allowing changes.

```jsx example
const OPTIONS = [
  { label: 'New York', value: 'ny' },
  { label: 'Los Angeles', value: 'la' },
  { label: 'Tokyo', value: 'tk' },
]
const [cities, setCities] = useState(['ny'])

return (
  <MultiSelect
    readonly
    value={cities}
    onChange={setCities}
    options={OPTIONS}
  />
)
```

## Tag

You can provide a custom tag component using the `TagComponent` prop. It receives `{ record, index, isLast }` where `record` is the matching option object.

```jsx example
const OPTIONS = [
  { label: 'New York', value: 'ny' },
  { label: 'Los Angeles', value: 'la' },
  { label: 'Tokyo', value: 'tk' },
]

const [cities, setCities] = useState([])

return (
  <MultiSelect
    value={cities}
    TagComponent={({ record }) => <Span>✔ {record.label} &nbsp;&nbsp;&nbsp;&nbsp;</Span>}
    onChange={setCities}
    options={OPTIONS}
  />
)
```

## Input component

You can provide a custom input component using the `InputComponent` prop. It receives `{ children, onOpen, onHide, value, placeholder, disabled, readonly, focused }`.

```jsx example
const OPTIONS = [
  { label: 'New York', value: 'ny' },
  { label: 'Los Angeles', value: 'la' },
  { label: 'Tokyo', value: 'tk' },
]

const [cities, setCities] = useState([])

return (
  <MultiSelect
    value={cities}
    InputComponent={
      ({ children, onOpen }) => {
        return (
          <Div
            onPress={onOpen}
            style={{
              border: '1px dotted green',
              padding: 8
            }}
          >
            {children}
          </Div>
        )
      }
    }
    TagComponent={({ record }) => <Span>✔ {record.label} &nbsp;&nbsp;&nbsp;&nbsp;</Span>}
    onChange={setCities}
    options={OPTIONS}
  />
)
```

## Tag limit

You can limit the number of displayed tags by passing `tagLimit`. Extra tags are collapsed and shown as a "+N" indicator.

```jsx example
const OPTIONS = [
  { label: 'New York', value: 'ny' },
  { label: 'Los Angeles', value: 'la' },
  { label: 'Tokyo', value: 'tk' },
]

const [cities, setCities] = useState([])

return (
  <MultiSelect
    tagLimit={2}
    value={cities}
    onChange={setCities}
    options={OPTIONS}
  />
)
```

## Limit the number of selected tags

You can limit the number of selections by passing `maxTagCount`. Once the limit is reached, additional options cannot be selected.

```jsx example
const OPTIONS = [
  { label: 'New York', value: 'ny' },
  { label: 'Los Angeles', value: 'la' },
  { label: 'Tokyo', value: 'tk' },
]

const [cities, setCities] = useState([])

return (
  <MultiSelect
    maxTagCount={2}
    value={cities}
    onChange={setCities}
    options={OPTIONS}
  />
)
```

## Props

- **value** -- an array of selected values (default `[]`)
- **options** -- an array of `{ label, value }` objects or primitives (default `[]`)
- **onChange** -- called with the updated array of values when the selection changes
- **onSelect** -- called with the value when an option is selected
- **onRemove** -- called with the value when an option is removed
- **onFocus** -- called when the dropdown opens
- **onBlur** -- called when the dropdown closes
- **placeholder** -- text shown when no options are selected (default `'Select'`)
- **disabled** -- disables all interactions (default `false`)
- **readonly** -- renders selected values as read-only text (default `false`)
- **tagLimit** -- maximum number of visible tags; extra tags show a "+N" indicator
- **tagLimitVariant** -- behavior when tags are limited: `'hidden'` or `'disabled'` (default `'hidden'`)
- **maxTagCount** -- maximum number of options that can be selected
- **TagComponent** -- a custom component for rendering tags
- **InputComponent** -- a custom component for rendering the input area
- **renderListItem** -- a custom renderer for dropdown items; receives `{ item, index, selected }`
- **hasWidthCaption** -- match the dropdown width to the input width on web (default `false`)
- **style** -- custom styles for the popover anchor wrapper
- **inputStyle** -- custom styles for the input wrapper
- **ref** -- a ref providing imperative `focus()` and `blur()` methods
<!-- CSSX_STYLE_REFERENCE_START -->
## Styling reference

Use these names from `StartupjsProvider style` or another CSSX provider style layer for app-wide overrides.

### Parts

| Part | Description |
| --- | --- |
| `root` | Root element. |
| `input` | Input control. |

### CSS variables

| Variable | Default | Description |
| --- | --- | --- |
| `--MultiSelect-input-bg` | `var(--color-background)` | Controls the input background. |
| `--MultiSelect-input-border-width` | `1px` | Controls the input border width. |
| `--MultiSelect-input-border-color` | `var(--color-border)` | Controls the input border color. |
| `--MultiSelect-input-bg-disabled` | `var(--color-muted)` | Controls the input background disabled. |
| `--MultiSelect-input-placeholder-color` | `var(--color-muted-foreground)` | Controls the input placeholder color. |
| `--MultiSelect-input-focused-border-color` | `var(--color-primary)` | Controls the input focused border color. |
| `--MultiSelect-input-error-border-color` | `var(--color-destructive)` | Controls the input error border color. |
| `--MultiSelect-input-padding-y` | `5px` | Controls the vertical input padding. |
| `--MultiSelect-input-padding-x` | `0.5rem` | Controls the horizontal input padding. |
| `--MultiSelect-input-min-height` | `2rem` | Controls the input min height. |
| `--MultiSelect-input-min-width` | `4rem` | Controls the input min width. |
| `--MultiSelect-input-radius` | `var(--radius-md)` | Controls the input radius. |
| `--MultiSelect-input-row-gap` | `0.25rem` | Controls the input row gap. |
| `--MultiSelect-tag-margin-right` | `0.25rem` | Controls the tag margin right. |
| `--MultiSelect-row-padding-right` | `1.5rem` | Controls the row padding right. |
| `--MultiSelect-suggestions-max-height` | `12.5rem` | Controls the suggestions max height. |
| `--MultiSelect-suggestions-padding-x` | `0.5rem` | Controls the horizontal suggestions padding. |
| `--MultiSelect-suggestions-native-padding-y` | `0.5rem` | Controls the vertical suggestions native padding. |
| `--MultiSelect-native-list-content-padding-top` | `2.5rem` | Controls the native list content padding top. |
| `--MultiSelect-swipe-bg` | `var(--color-secondary)` | Controls the swipe background. |
| `--MultiSelect-suggestion-padding-y` | `0.5rem` | Controls the vertical suggestion padding. |
| `--MultiSelect-suggestion-padding-x` | `1rem` | Controls the horizontal suggestion padding. |
| `--MultiSelect-suggestion-padding-desktop` | `0.5rem` | Controls the suggestion padding desktop. |
| `--MultiSelect-suggestion-text-color` | `var(--color-foreground)` | Controls the suggestion text color. |
| `--MultiSelect-suggestion-text-font-size` | `var(--text-xl)` | Controls the suggestion text font size. |
| `--MultiSelect-suggestion-text-line-height` | `1.75rem` | Controls the suggestion text line height. |
| `--MultiSelect-suggestion-text-font-size-desktop` | `var(--text-sm)` | Controls the suggestion text font size desktop. |
| `--MultiSelect-suggestion-text-line-height-desktop` | `1.25rem` | Controls the suggestion text line height desktop. |
| `--MultiSelect-backdrop-z-index` | `1501` | Controls the backdrop z index. |
| `--MultiSelect-popover-padding-x` | `0.25rem` | Controls the horizontal popover padding. |
| `--MultiSelect-popover-min-width` | `10rem` | Controls the popover min width. |
| `--MultiSelect-ellipsis-margin-x` | `0.5rem` | Controls the horizontal ellipsis margin. |
| `--MultiSelect-suggestion-item-padding-y` | `0.5rem` | Controls the vertical suggestion item padding. |
| `--MultiSelect-check-width` | `1rem` | Controls the check width. |
| `--MultiSelect-check-icon-color` | `var(--color-primary)` | Controls the check icon color. |
| `--MultiSelect-label-margin-right` | `0.5rem` | Controls the label margin right. |
<!-- CSSX_STYLE_REFERENCE_END -->

## Sandbox

<Sandbox
  Component={MultiSelect}
  propsJsonSchema={MultiSelectPropsJsonSchema}
  props={{
    value: ['New York'],
    options: ['New York', 'Los Angeles', 'Tokyo'],
    onChange: value => console.info('New value is ' + JSON.stringify(value)),
    onSelect: value => console.info('Value "' + value + '" is selected'),
    onRemove: value => console.info('Value "' + value + '" is removed'),
  }}
  block
/>
